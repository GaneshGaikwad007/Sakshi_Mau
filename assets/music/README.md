# Music Notes

This project does not include or host copyrighted songs.

The gallery uses external embedded YouTube playback and credit/search links configured in `scripts/config.js`.
To replace a song, paste a legal embed URL from YouTube, Spotify, or another provider into the matching `embedUrl` field and update the `song`, `credit`, and credit link text if needed.
